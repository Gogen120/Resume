language: python
python:
  - 3.6
script:
  - make html
before_deploy:
  - cd build/html/
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GH_TOKEN # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  file:
    - build/html
  on:
    branch: master
